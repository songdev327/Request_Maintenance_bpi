const config = {
  api_path: 'http://localhost:3005',

};

export default config;

// const config = {
//   api_path: `${window.location.protocol}//${window.location.hostname}:${window.location.port}`,

// };

// export default config;

// const config = {
//   // ✅ ชี้ไปที่ Backend Server ตลอด ไม่ว่า user จะเข้าผ่าน IP ไหน
//   api_path: "http://192.168.96.126:3005",
// };

// export default config;